sap.ui.define([],
    function(){
        return {
            captialize:function(word){
                if(word){
                    return word.toUpperCase();
                }
            }
        };
    }
);